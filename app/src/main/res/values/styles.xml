<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.MyAppTheme" parent="@style/Theme.AppCompat.Light">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/blue</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="MainLayoutStartEndStyle">
        <item name="android:paddingLeft">@dimen/eight_dp_margin</item>
        <item name="android:paddingRight">@dimen/fab_margin</item>
    </style>

    <style name="DescriptionGravityStyle">
        <item name="android:layout_gravity">left</item>
    </style>

    <style name="NetworkImageLeftToRightStyle">
        <item name="android:layout_gravity">right|bottom</item>
        <item name="android:layout_marginLeft">@dimen/four_dp_margin</item>
    </style>

    <style name="FloatingActionStyle">
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_marginRight">@dimen/twelve_dp_margin</item>
    </style>

</resources>
